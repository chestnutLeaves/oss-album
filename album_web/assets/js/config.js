/**
 * ============================================================================
 * 个人相册项目 - 全局配置文件
 * ============================================================================
 * 
 * 本文件集中管理项目中的所有可配置项，便于统一维护和修改。
 * 修改配置后，所有引用该配置的模块会自动生效。
 * 
 * @version 1.0.0
 * @author 项目开发团队
 */

/**
 * ----------------------------------------------------------------------------
 * API 接口配置
 * ----------------------------------------------------------------------------
 * 用于配置与后端服务通信的相关参数
 */
export const API_CONFIG = {
    /**
     * API 基础地址
     * 说明：后端服务的根地址，所有 API 请求都会基于此地址
     * 示例：'http://localhost:8888' 或 'https://api.example.com'
     */
    BASE_URL: 'http://localhost:8888'
};

/**
 * ----------------------------------------------------------------------------
 * 站点默认配置
 * ----------------------------------------------------------------------------
 * 当后端接口无法获取配置时，使用以下默认值
 */
export const SITE_CONFIG = {
    /**
     * 站点默认描述
     * 说明：显示在页面头部副标题位置
     */
    DEFAULT_DESCRIPTION: '记录生活中的美好瞬间',
    
    /**
     * 页面标题后缀
     * 说明：所有页面标题后面附加的后缀
     */
    TITLE_SUFFIX: ' - 个人相册'
};

/**
 * ----------------------------------------------------------------------------
 * UI 交互配置
 * ----------------------------------------------------------------------------
 * 控制页面交互行为和动画效果
 */
export const UI_CONFIG = {
    /**
     * 轮播图配置
     */
    CAROUSEL: {
        /**
         * Banner 轮播图自动播放间隔（毫秒）
         * 说明：首页顶部大图的自动切换时间
         */
        BANNER_AUTOPLAY_INTERVAL: 3000,
        
        /**
         * 相册封面轮播图自动播放间隔（毫秒）
         * 说明：相册卡片封面的自动切换时间
         */
        ALBUM_AUTOPLAY_INTERVAL: 4000
    },
    
    /**
     * 搜索配置
     */
    SEARCH: {
        /**
         * 搜索防抖延迟（毫秒）
         * 说明：用户输入后延迟多久触发搜索，避免频繁请求
         */
        DEBOUNCE_DELAY: 300
    },
    
    /**
     * 标签配置
     */
    TAGS: {
        /**
         * 卡片上最多显示的标签数量
         * 说明：超过此数量将显示 "+N" 按钮
         */
        MAX_DISPLAY_TAGS: 6
    },
    
    /**
     * 加载和提示配置
     */
    FEEDBACK: {
        /**
         * 错误提示自动关闭时间（毫秒）
         * 说明：错误消息显示多久后自动消失
         */
        ERROR_AUTO_HIDE_DELAY: 3000
    }
};

/**
 * ----------------------------------------------------------------------------
 * 缓存配置
 * ----------------------------------------------------------------------------
 * 控制本地存储的相关设置
 */
export const CACHE_CONFIG = {
    /**
     * 相册密码缓存过期时间（毫秒）
     * 说明：访问加密相册的密码缓存有效期
     * 默认值：3天 = 3 * 24 * 60 * 60 * 1000
     */
    PASSWORD_EXPIRES: 3 * 24 * 60 * 60 * 1000,
    
    /**
     * 相册密码存储键名
     */
    PASSWORD_STORAGE_KEY: 'albumPasswords'
};

/**
 * ----------------------------------------------------------------------------
 * 错误页面配置
 * ----------------------------------------------------------------------------
 * 错误跳转页面的路径配置
 */
export const ERROR_PAGES = {
    /**
     * 网络错误页面
     * 说明：当 API 请求网络失败时跳转
     */
    NETWORK_ERROR: 'error/error-network.html',
    
    /**
     * 站点不存在页面
     * 说明：当站点配置不存在时跳转
     */
    SITE_NOT_FOUND: 'error/error-site.html'
};

/**
 * ----------------------------------------------------------------------------
 * 导出完整配置对象（供需要整体配置的场景使用）
 * ----------------------------------------------------------------------------
 */
export const CONFIG = {
    API: API_CONFIG,
    SITE: SITE_CONFIG,
    UI: UI_CONFIG,
    CACHE: CACHE_CONFIG,
    ERROR: ERROR_PAGES
};

/**
 * 默认导出完整配置
 */
export default CONFIG;
